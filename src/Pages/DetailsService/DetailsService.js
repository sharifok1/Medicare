import React from 'react';

const DetailsService = ({children}) => {
    console.log(children)
    return (
        <div>
            <h1>this is Details</h1>
        </div>
    );
};

export default DetailsService;